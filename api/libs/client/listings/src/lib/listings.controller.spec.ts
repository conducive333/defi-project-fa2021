import { Test, TestingModule } from '@nestjs/testing'
import { ClientListingsController } from './listings.controller'

describe('ClientListingsController', () => {
  let controller: ClientListingsController

  // beforeEach(async () => {
  //   const module: TestingModule = await Test.createTestingModule({
  //     controllers: [ClientListingsController],
  //   }).compile();

  //   controller = module.get<ClientListingsController>(ClientListingsController);
  // });

  // it('should be defined', () => {
  //   expect(controller).toBeDefined();
  // });
})
